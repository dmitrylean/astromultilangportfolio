---
interface Props {
  categories: string[];
  currentLang: string;
  catDict: Record<string, string>;
}

const { categories, currentLang, catDict } = Astro.props;
---

<section class="categories-filter">
  <div class="container">
    <ul class="categories-list">
      <li>
        <a href={`/${currentLang}/blog`}>All</a>
      </li>
      {
        categories.map((cat) => (
          <li>
            <a href={`/${currentLang}/category/${cat}`}>
              {catDict[cat] ?? cat}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</section>
